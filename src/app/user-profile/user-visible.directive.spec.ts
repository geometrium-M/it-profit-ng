import { UserVisibleDirective } from './user-visible.directive';

describe('UserVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new UserVisibleDirective();
    expect(directive).toBeTruthy();
  });
});


