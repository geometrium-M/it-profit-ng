import { Zoom2DirectiveDirective } from './zoom2-directive';

describe('Zoom2DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Zoom2DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
